// Shown, not live (per spec): a one-line nod to the SRE's broader, continuous capability.
export function DriftTab() {
  return (
    <div className="drift" title="Roadmap capability — shown, not live in this demo">
      <span className="drift-dot" />
      Drift watch: detected an intermittent tool-selection issue across 14 traces (triage only)
    </div>
  );
}
